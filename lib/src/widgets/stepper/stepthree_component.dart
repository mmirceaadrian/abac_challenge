import 'package:flutter/material.dart';

Step buildStepThree() {
  return Step(
    label: Text('Confirma'),
    title: const Text(''),
    content: Text('Content for Step 3'),
    isActive: true,
  );
}
